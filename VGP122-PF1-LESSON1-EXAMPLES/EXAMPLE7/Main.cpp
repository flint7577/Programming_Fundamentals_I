#include <iostream>

int main()
{
	// Calculate the value using order of operation rules
	int y = (2 * 5 * 5) + (3 * (5 + 7));

	// Display the result of the calculation
	std::cout << "The calculation returned " << y << std::endl;

	return 0;
}
